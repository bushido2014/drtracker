<div{{ attributes.addClass('job-content') }}>
{% if not label_hidden %}
    <h3{{ title_attributes }}>{{ label }}</h3>
  {% endif %}
{% for item in items %}
    <div class="jobs-text">
      {{ item.content }}
    </div>
  {% endfor %}
</div>
