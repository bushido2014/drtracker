<div{{ attributes.addClass('link-outline') }}>
  {% if not label_hidden %}
    <h3{{ title_attributes }}>{{ label }}</h3>
  {% endif %}

  {% for item in items %}
    <a href="{{ item.content['#url'] }}" class="btn btn-outline">
      {{ item.content['#title'] }}
    </a>
  {% endfor %}
</div>
